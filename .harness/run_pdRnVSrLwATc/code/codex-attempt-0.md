I’m reading the repo instructions and the daemon boot path first, then I’ll patch the startup order so `initialize` comes up before the heavy awaits while preserving the existing audit fail-closed behavior.I’ve confirmed the regression point: several awaited storage and provider initializers still run before `startMcpServer`. Next I’m reading the boot function and existing daemon tests so I can move only the blocking pieces and add a regression test around startup ordering.The boot path is all in one `main()` today, so I’m checking what can be factored without broad changes and where to anchor a startup regression test against the MCP server layer.I’ve got the shape of the fix now: transport first, storage/bootstrap in the background, and provider objects turned into lazy proxies so a slow model endpoint can’t hold `initialize` hostage. I’m checking the test harness and scripts next, then I’ll patch the daemon and add a regression test around startup ordering.I’m patching the startup seam now. One subtlety matters here: moving work “after `startMcpServer`” is not enough if the next line immediately does a long synchronous migration, because that still blocks the event loop before the client’s `initialize` gets serviced. The fix needs an actual event-loop yield after transport attach.I’m moving the DB/schema/seed/audit path behind the transport attach now, and I’m keeping the gate fail-closed if any of that deferred bootstrap fails. The provider probes are being moved onto lazy wrappers plus a detached warm-up, so tool paths can opt in when they actually need them.I’ve finished the code movement. I’m adding regression tests in two layers: one on the transport-first scheduler itself, and one on the lazy provider proxies so construction/probing is genuinely deferred until after boot.I’ve got the implementation and regression tests in place. I’m running the daemon build first to catch type issues from the startup refactor, then I’ll run the full daemon Vitest suite as required.The build is clean. The full Vitest run hit an environment limitation here: default worker startup is failing with `spawn EPERM`, so I’m rerunning the same suite in a single-worker/threaded mode to get an actual pass/fail signal on the tests themselves rather than on process creation.Vitest itself is blocked here by the sandboxed Windows environment calling `net use` inside Vite’s path resolver, so I’m adding one direct Node smoke check against the compiled boot scheduler to verify the transport-first sequencing path still behaves as intended.Implemented the startup refactor so the MCP stdio transport is attached before the heavy boot path, and the post-attach work is scheduled onto the next event-loop turn so a cold `sql.migrate()` cannot still starve `initialize`. The audit gate is still fail-closed: tools stay refused until the deferred bootstrap finishes `audit.verifyChain()`. Provider construction and availability probes are now lazy/backgrounded via wrappers, so slow embedder/completer endpoints no longer sit on the handshake path.

Changed files:
- [daemon/src/index.ts](/C:/AiAppDeployments/TheEights/daemon/src/index.ts:186) wires `LazyEmbedder`/`LazyCompleter`, moves migrate/graph/seed/audit work into deferred bootstrap, and starts transport through the new transport-first scheduler.
- [daemon/src/boot-sequencing.ts](/C:/AiAppDeployments/TheEights/daemon/src/boot-sequencing.ts:1) adds the `setImmediate` transport-first boot helper.
- [daemon/src/providers/lazy.ts](/C:/AiAppDeployments/TheEights/daemon/src/providers/lazy.ts:1) adds lazy embedder/completer proxies that construct/probe on demand.
- [daemon/test/boot-sequencing.test.ts](/C:/AiAppDeployments/TheEights/daemon/test/boot-sequencing.test.ts:1) adds the regression test for “transport first, heavy work deferred”.
- [daemon/test/lazy-providers.test.ts](/C:/AiAppDeployments/TheEights/daemon/test/lazy-providers.test.ts:1) adds coverage that provider construction is deferred until first use.

Checks run:
- `npm run build` in `daemon/`: passed.
- `node --input-type=module -` smoke check against compiled `dist/boot-sequencing.js`: passed, output `transport,warm,bootstrap`.
- `npm test` in `daemon/`: blocked by this environment. Vitest/Vite aborts before collecting tests with `spawn EPERM` from Vite’s Windows realpath path (`net use` exec), so I could not get the full daemon suite to execute here.