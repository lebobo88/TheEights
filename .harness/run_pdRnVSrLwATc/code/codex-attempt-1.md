I’m checking the repo instructions and the daemon boot path first, then I’ll patch the startup order and add tests around the initialize handshake behavior.The current `index.ts` already has some lazy-provider scaffolding, so I’m reading the exact boot sequence before changing anything. Next I’m checking the handshake path, boot helpers, and existing daemon tests to keep the patch surgical.The daemon already has a transport-first helper and lazy wrappers; I’m checking whether `main()` still lets anything heavy slip onto the connect path and whether there’s already a test seam I can extend instead of introducing a bigger refactor.I’ve confirmed the current file already moved the heavy work behind a deferred bootstrap, but the proof is only at the helper level. I’m now tightening the implementation seam in `index.ts` itself and adding a test that specifically guards against provider availability on the initialize path.I’m checking whether the repo already contains a partial version of this fix. If it does, I’ll avoid churning the boot logic and only add the missing seam/tests needed to lock the behavior down.The worktree already has an uncommitted partial fix in exactly the startup files this task touches. I’m diffing that state now so I can refine it rather than overwrite it.The remaining risk is the event-loop race itself: deferring heavy boot to `setImmediate()` can still win the next turn before the client’s `initialize` bytes are processed. I’m shifting bootstrap behind a short handshake grace window and adding a unit test around that specific behavior.I’m editing the boot helper and its tests now. The code change is small: give the MCP transport a short grace window before any synchronous bootstrap work can run, and codify that provider warmup is background-only.The code change is in place. I’m running the focused tests first to catch any behavioral or typing issues before I run the full daemon suite and build.The build is clean. The first test command used repo-root paths from inside `daemon/`, so I’m rerunning the focused tests with the correct relative paths and then the full daemon suite.Vitest is blocked here by process-spawn permissions rather than failing tests. I’m rerunning it in thread mode, which usually works in this sandbox, and I’ll use the same mode for the full daemon suite if it holds.The remaining blocker is environmental: Vite/Vitest is trying to spawn helper processes for Windows realpath resolution, which this sandbox forbids. I’m checking for a documented switch in the installed Vite code so I can still execute the suite locally without changing repo code.I found the culprit in the installed Vite bundle: on Windows it shells out to `net use` during path resolution. I’m using a temporary preload shim for the test run so I can still execute Vitest in this sandbox without changing the repo.Vitest itself can’t execute in this sandbox because both Vite’s Windows path probe and esbuild’s transform service try to spawn child processes. I’m still running a direct Node smoke check against the compiled boot helper so I can verify the new handshake behavior, and I’ll report the Vitest limitation explicitly.The direct smoke check passed. I’m cleaning up the temporary test shim now so the repo only contains the actual source/test changes for this fix.The transport-first boot path now has a real handshake window instead of relying on `setImmediate()`. In [daemon/src/boot-sequencing.ts](/C:/AiAppDeployments/TheEights/daemon/src/boot-sequencing.ts) I changed deferred boot to wait through a short 250ms grace period after `startTransport()` before kicking off provider warmup or any heavy bootstrap work. That closes the remaining event-loop race where `sql.migrate()` or graph startup could still run before AgentSmith’s `initialize` bytes were serviced. The fail-closed audit gate remains in the existing [daemon/src/index.ts](/C:/AiAppDeployments/TheEights/daemon/src/index.ts) flow: transport comes up first, but audited tools stay refused until `audit.verifyChain()` passes.

I also tightened the proof in [daemon/test/boot-sequencing.test.ts](/C:/AiAppDeployments/TheEights/daemon/test/boot-sequencing.test.ts). The tests now assert that:
- transport attach returns before the grace window expires,
- embedder/completer availability probes are not awaited on the initialize path,
- deferred bootstrap failures still do not reject transport attach.

Checks I ran:
- `npm run build` in `daemon/`: passed.
- Direct smoke check against `dist/boot-sequencing.js`: passed (`boot-sequencing smoke ok`), confirming transport returns immediately and bootstrap waits until after the grace window.
- `npm test` / `vitest run`: could not complete in this sandbox. Vite/esbuild on Windows tries to spawn helper processes and fails here with `spawn EPERM`, so I could not get a trustworthy full-suite result from this environment.